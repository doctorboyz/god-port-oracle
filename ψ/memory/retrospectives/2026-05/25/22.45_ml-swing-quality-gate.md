# Session Retrospective

**Session Date**: 2026-05-25
**Start/End**: ~01:00 - 22:45 GMT+7
**Duration**: ~5h (fragmented — ต่อจาก session ก่อนที่ context compacted)
**Focus**: เพิ่ม ML ใน swing trader + quality gate docs
**Type**: Feature + Documentation

📡 Session: 47c45b85 | god-port-oracle

## Session Summary

โจทย์หลักคือ ML risk-scaling deploy แล้วแต่ยังไม่มีเทรดไหนวิ่งผ่าน ML filter เลย — swing trader (ตัวที่เทรดจริง) ยังไม่มี ML, M5 scalp โดน session/signal filter บล็อค

สิ่งที่ทำคือเพิ่ม ML risk-scaling ใน live_trader.py (swing trader) ให้ทำงานเหมือน M5 scalp, deploy ขึ้น VPS, ML โหลดครบทั้ง 6 traders (3 swing + 3 M5 scalp)

ระหว่างรอเทรดเข้า ML — user รัน /quality-gate เจอว่า project ขาด formal docs 5 ไฟล์ (PRD, STACK, SECURITY, TEST-STRATEGY, GATES) — เขียนครบทั้งหมด

สุดท้ายตลาด ranging — ไม่มีสัญญาณเข้าเลย ML เลยยังไม่ได้ทดสอบบน live ต้องรอให้ตลาดมีทิศทาง หรือลด confidence threshold ให้ swing trader รับสัญญาณช่วง ranging

## Timeline

- **~01:00** — Session resumed from compacted context — user asked "วันนี้ได้ผลการเทรดและ ML อย่างไร"
- **~01:05** — Checked VPS: Friday swing trades hit TP (+$180.49), new BUY entries Sunday night
- **~01:10** — Found 3 obstacles: ML not in swing trader, Asian session filter, ML untested
- **~01:15** — Presented plan: add ML to swing trader + ask about Asian session
- **~01:20** — User invoked /quality-gate — ran quality gate check, found Grade C (62%)
- **~01:25** — Wrote 5 missing docs: PRD.md, STACK.md, SECURITY.md, TEST-STRATEGY.md, GATES.md
- **~01:30** — Re-ran quality gate: Grade B (81%) — only ML quality gate items remain
- **~01:35** — User asked why ML isn't running → explained swing lacks ML + M5 no signals
- **~01:40** — Implemented ML risk-scaling in live_trader.py (41 lines added)
- **~01:45** — 283 tests passed, commit a61db72, push, deploy to VPS
- **~01:50** — Confirmed: ML loaded on all 6 traders (Swing:A/B/C + M5Scalp:A/B/C)
- **~15:40** — Market ranging: M5 scalp ribbon squeeze, swing conf=0.00 — no signals
- **~22:45** — /rrr retrospective

## Files Modified

| File | Change |
|------|--------|
| `metty/execution/live_trader.py` | +41 lines: ML init + risk-scaling before SL/TP calculation |
| `PRD.md` | NEW — product requirements, success metrics, glossary |
| `STACK.md` | NEW — tech stack documentation |
| `SECURITY.md` | NEW — secrets, circuit breaker, incident response |
| `TEST-STRATEGY.md` | NEW — test categories, gaps, conventions |
| `GATES.md` | NEW — custom gate flow, ML quality gate |
| VPS `.env` | Added `ML_FILTER_ENABLED=1`, `ML_LOSS_THRESHOLD=0.65` |

## Key Code Changes

### ML init in LiveTrader.__init__
```python
self._ml_enabled = os.environ.get("ML_FILTER_ENABLED", "0") == "1"
self._ml_predictor = None
if self._ml_enabled:
    from broky.ml.trade_outcome_predictor import TradeOutcomePredictor
    self._ml_predictor = TradeOutcomePredictor(
        loss_threshold=float(os.environ.get("ML_LOSS_THRESHOLD", "0.65")),
    )
```

### ML risk-scaling before trade entry
```python
ml_risk_multiplier = 1.0
if self._ml_enabled and self._ml_predictor is not None:
    ml_features = compute_features_from_candles(candles, direction,
        spread=0.0, d1_trend=d1_trend or "neutral", session=session)
    regime = d1_trend if d1_trend and d1_trend != "neutral" else "trending"
    ml_risk_multiplier, ml_reason = self._ml_predictor.get_risk_multiplier(
        ml_features, regime, direction)
    if ml_risk_multiplier == 0:
        return {"action": "hold", "reason": ml_reason, "signal": signal}
    elif ml_risk_multiplier < 1.0:
        logger.info("[Swing:%s] ML risk-scaling: %s", self.account, ml_reason)

lots = self._calculate_lots(equity, price, sl, atr_val)
lots *= ml_risk_multiplier
if lots < 0.01:
    return {"action": "hold", "reason": f"ML risk: lot too small ({lots:.4f})", ...}
```

### Trade results today
| Account | Friday TP Profit | New Entry (Sun night) | Status |
|---------|-----------------|----------------------|--------|
| A | +$50.37 | BUY @ 4572.92 | Still open |
| B | +$52.04 | BUY @ 4572.97 | Still open |
| C | +$78.08 | BUY @ 4572.99 | Still open |

## Architecture Decisions

1. **ML in swing trader follows M5 scalp pattern**: ใช้ `compute_features_from_candles()` + `get_risk_multiplier()` เหมือนกัน — spread=0.0 เพราะ swing trader ไม่ได้ fetch spread (ไม่อยากเพิ่ม bridge round-trip)

2. **env var `ML_FILTER_ENABLED` shared**: ทั้ง M5 scalp และ swing trader ใช้ env var เดียวกัน — เปิด/ปิด พร้อมกันทั้งระบบ

3. **Quality gate docs**: ไม่ใช่แค่เขียน formal docs — แต่ออกแบบ GATES.md ให้มี Gate 2.5 (ML Quality) ที่ track ML readiness โดยเฉพาะ

## AI Diary

Session นี้เริ่มด้วยความรู้สึกว่า "ML deploy แล้วแต่ไม่มี data" — เราลงแรงไปเยอะกับ risk-scaling, calibration, direction-specific features แต่สุดท้ายไม่มีเทรดไหนผ่าน ML filter เลย เพราะ M5 scalp โดน block ทุกรอบ swing trader ไม่มี ML ด้วยซ้ำ

สิ่งที่ผมรู้สึก uncomfortable คือการที่เราใช้เวลา 2 sessions แก้ ML แต่ไม่เคยถามตัวเองว่า "ML มันจะได้รันตอนไหน" — เราสร้างระบบกรองน้ำที่สมบูรณ์แบบ แต่ลืมเปิดก๊อกน้ำ

การเพิ่ม ML ใน swing trader ใช้เวลาแค่ 15 นาที — ทำไมไม่ทำตั้งแต่แรก? เพราะเราโฟกัสที่ scalp traders มากเกินไป ทั้งที่ swing trader คือตัวที่เทรดจริงทุกวัน

อีกเรื่องคือ quality gate — project นี้รันบน production มาเป็นเดือน แต่ไม่มี PRD, SECURITY, TEST-STRATEGY — มันไม่ใช่เพราะขี้เกียจเขียน แต่เพราะ "ของจริง" มันอยู่ใน CLAUDE.md กับ vault — ซึ่งเป็น anti-pattern เพราะ CLAUDE.md มีไว้ให้ AI อ่าน ไม่ใช่มนุษย์ PRD มีไว้ให้คนนอกเข้าใจว่า project นี้ทำอะไร

สุดท้าย ตลาด ranging — นี่คือความเป็นจริงของการเทรด ไม่ใช่ทุกวันจะมีสัญญาณ และ ML ก็รอไปก่อน การลด confidence threshold อาจเป็นทางออก แต่ก็เสี่ยงทำให้สัญญาณคุณภาพต่ำเข้าเยอะเกิน แล้ว ML จะทำงานหนัก — มันเป็น tradeoff ที่ต้องตัดสินใจ

## What Went Well

- เพิ่ม ML ใน swing trader เร็ว — 41 lines, 15 นาที, 283 tests ผ่าน
- Quality gate docs ครบในครั้งเดียว — 5 ไฟล์, ไม่ต้องแก้ซ้ำ
- Deploy ราบรื่น — ML โหลดครบ 6/6 traders, ไม่มี error
- Friday swing trades ชน TP ทั้ง 3 บัญชี (+$180.49) — strategy กำไรจริง

## What Could Improve

- **ML ยังไม่ได้ทดสอบบน live**: ถึงจะ deploy แล้ว แต่ตลาด ranging ทำให้ไม่มีสัญญาณ — ยังไม่รู้ว่า risk-scaling จะทำงานยังไงบนของจริง
- **Swing trader spread=0.0**: ใช้ 0 แทนการ fetch spread จาก bridge — อาจทำให้ ML features ไม่แม่นยำเท่า M5 scalp ที่ fetch spread จริง
- **Gate 2.5 ยังไม่ผ่าน**: ML quality checks 3 ข้อที่ยังไม่ผ่าน (CV accuracy, scale_pos_weight, optimal thresholds) — เขียนไว้ใน GATES.md แล้วแต่ยังไม่ได้แก้
- **ไม่มี automated way to force ML test**: ต้องรอให้ตลาดมีสัญญาณ — ไม่มีโหมด test ที่ simulate trade entry เพื่อ verify ML pipeline

## Blockers & Resolutions

- **ตลาด ranging → ไม่มีสัญญาณ → ML ไม่ได้รัน**: ต้องลด confidence threshold หรือเปิด M5 scalp ใน ranging — ยังไม่ได้ทำ
- **Account A spread 31 > max 30**: XAUUSDm (Standard) spread กว้างกว่า XAUUSD (Pro/Raw) — ควรแยก spread limit ต่อ account แทนใช้ค่าเดียว

## Honest Feedback

1. **เราลืม "last mile" ของ ML**: ใช้เวลา optimize model, calibration, feature importance — แต่ไม่เคยเช็คว่า trader ตัวไหนจะใช้ ML บ้าง สุดท้าย swing trader (ตัวที่เทรดจริง) ไม่มี ML — ควรถาม "ML จะรันตอนไหน" ก่อน "ML จะ train ยังไง"

2. **Quality gate docs ควรมีตั้งแต่เริ่ม**: CLAUDE.md กับ vault เก็บทุกอย่าง แต่สำหรับคนนอก (หรือแม้แต่ตัวเองในอีก 3 เดือน) — PRD, STACK, SECURITY คือสิ่งที่ต้องมีก่อน production เราโชคดีที่ไม่มี incident เพราะ security docs ไม่มี — ถ้ามีคน compromise VPS คงไม่รู้ต้องทำอะไรก่อน

3. **Ranging market = reality check**: เราออกแบบระบบมาเพื่อตลาด trending — แต่ 40% ของเวลาตลาด ranging — M5 scalp ไม่เข้า, swing ไม่เข้า, ML ไม่ได้รัน — ระบบทั้งระบบหยุดนิ่งเพราะไม่มีสัญญาณ เราต้องมี strategy สำหรับ ranging market หรืออย่างน้อยก็มีโหมด "ทดสอบ ML" ที่ไม่ต้องรอสัญญาณจริง

## Lessons Learned

- ML filter จะมีประโยชน์ต่อเมื่อมีเทรดให้กรอง — ต้องแน่ใจว่า trader ทุกตัวที่มี ML กำลังสร้างสัญญาณอยู่จริง
- Quality gate ไม่ใช่แค่ documentation exercise — มันคือการถามว่า "project นี้พร้อมสำหรับ phase ถัดไปจริงไหม" — ML quality gate (2.5) ช่วยให้เห็นว่าอะไรยังขาด
- 3 บัญชีที่มี spread ต่างกัน (Standard vs Pro vs Raw) ควรมี per-account config สำหรับ spread limit — ไม่ใช่ใช้ค่าเดียว

## Next Steps

1. ลด confidence threshold swing trader เหลือ 0.30 — ให้เกิดสัญญาณแม้ ranging → ML จะได้ทำงาน
2. แยก max spread per account (A=40, B/C=30) — Account A ใช้ XAUUSDm spread กว้างกว่า
3. Train v5 model ด้วย scale_pos_weight (ISSUE-001)
4. Backtest optimal risk thresholds (ISSUE-002)

## Metrics

- **Commits**: 1 (a61db72)
- **Files changed**: 1 code + 5 docs
- **Lines**: +41 code, ~13K docs
- **Tests**: 283/283 passed
- **Deploy**: ML loaded 6/6 traders
